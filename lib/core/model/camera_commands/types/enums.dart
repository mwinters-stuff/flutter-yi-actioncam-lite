enum LiveVideoBitrate {
  Auto,
  Low,
  Middle,
  High
}

enum LiveVideoResolution
{
  r_480p,
  r_720p,
  r_1080p
}

enum SystemMode
{
  Unknown,
  Record,
  Capture
}

enum VideoOrPhoto { photo, video }

enum ToggleStateSettings {
  electronicImageStabilization,
  videoMute,
  buzzerRing,
  adjustLensDistortion
}